# מדריך ניתוח המסלולים והחישובים הגיאומטריים

קובץ זה מספק תיעוד מעמיק על אופן פעולת מנוע החישובים [`analyze_trajectories.py`](analyze_trajectories.py), מבנה הנתונים בטבלת הסיכום [`geometry_summary.csv`](geometry_summary.csv), ואלגוריתמי הערכת העקמומיות, הקינמטיקה והסיווג בפרויקט.

---

## 🔄 תהליך הניתוח וזרימת העבודה (Pipeline)

1. **איסוף:** [`mouse_tracker.py`](mouse_tracker.py) מקליט מסלולים גולמיים בפורמט CSV ושומר אותם תחת `data/session_XX/`.
2. **עיבוד וניתוח גיאומטרי:** [`analyze_trajectories.py`](analyze_trajectories.py) סורק את כל תיקיות ה-sessions, מחלץ תכונות גיאומטריות וקינמטיות מכל קובץ, ומייצר/מעדכן את [`geometry_summary.csv`](geometry_summary.csv).
3. **סיווג (Machine Learning):** [`classify_trajectories.py`](classify_trajectories.py) טוען את קובץ הסיכום, מסנן תצפיות תקינות, ומאמן עצי החלטה לסיווג אדם מול בוטים (בינארי ומרובה מחלקות).
4. **ויזואליזציה:** [`visualize_results.py`](visualize_results.py) מפיק תרשימים השוואתיים לתיקיית `plots/`.

---

## 🤖 סוגי המקורות (Source Types)

עמודת `source_type` בנתונים מבדילה בין אדם לחמשת סוגי הבוטים:
* `human`: משתמש אנושי (הקלטות ישנות ללא העמודה מוגדרות כאדם כברירת מחדל).
* `bot_linear`: בוט ישר במהירות קבועה.
* `bot_curved`: בוט נע על קשת בזייה ריבועית במהירות פרמטרית קבועה.
* `bot_noisy`: בוט עם הפרעות צידיות רציפות (Smoothstep) ותיקון הדרגתי.
* `bot_smart_jerk`: בוט עם פרופיל מהירות Minimum Jerk (פולינום מעלה 5) לאורך קשת בזייה.
* `bot_smart_full`: בוט ביומכני עם פרופיל מהירות א-סימטרי ($\tau^{0.8}$), בלימה מוארכת ורעידות נוירו-מוטוריות (10Hz ו-18Hz).

---

## 📊 פירוט שדות טבלת הסיכום (`geometry_summary.csv`)

### 1. מדדי ישרות, דרך ומרחב
* `path_length`: אורך המסלול הגולמי המצטבר בפיקסלים, מחושב כסכום המרחקים האוקלידיים בין דגימות רצופות: $\sum_{i=1}^{n-1} \sqrt{(x_{i+1}-x_i)^2 + (y_{i+1}-y_i)^2}$.
* `direct_distance`: המרחק האוקלידי הישיר בין הדגימה הראשונה לאחרונה: $\sqrt{(x_n-x_1)^2 + (y_n-y_1)^2}$.
* `target_center_distance`: המרחק בין מרכז מעגל ההתחלה הירוק למרכז מעגל המטרה האדום.
* `path_ratio`: היחס $\frac{\text{path\_length}}{\text{direct\_distance}}$. שווה ל-1.0 עבור קו ישר לחלוטין, וגדול מ-1 עבור מסלולים מעוקלים או רועשים.
* `max_chord_dev_px`: הסטייה המקסימלית הניצבת (בפיקסלים) של נקודות המסלול מהמיתר הישר המחבר בין נקודת ההתחלה לנקודת הסיום. קרוב ל-0 בבוט ליניארי, בינוני בבוט רועש, וגבוה בבוט מעוקל.

### 2. מדדי זמן וקינמטיקה
* `duration_s`: משך זמן התנועה הכולל בשניות (ההפרש בין דגימת הזמן האחרונה לראשונה: $t_n - t_1$).
* `mean_speed_px_s`: מהירות ממוצעת בפיקסלים לשנייה: $\frac{\text{path\_length}}{\text{duration\_s}}$.
* `stationary_interval_s`: סכום מרווחי הזמן $\Delta t$ שבהם מיקום העכבר לא השתנה ($\Delta x = \Delta y = 0$). מייצג זמני שהייה ועצירות ביניים.
* `median_sample_interval_ms`, `max_sample_interval_ms`: מרווח הדגימה החציוני והמרבי במילישניות.
* `peak_to_mean_speed`: היחס בין שיא המהירות הרגעית למהירות הממוצעת לאורך 20 פרוסות זמן שוות.
  * בבוטים בעלי מהירות אחידה (`linear`, `curved`, `noisy`): היחס קרוב ל-**1.0**.
  * בבוט Minimum Jerk (`bot_smart_jerk`): היחס התיאורטי הוא **1.875**.
  * בתנועה אנושית (`human`): היחס נע בדרך כלל בין **2.0 ל-2.6** (פרופיל פעמוני מובהק עם האצה ובלימה).

### 3. מדדי עקמומיות מקומית (Curvature Metrics)
* `mean_curvature`: ממוצע העקמומיות המקומית המוחלטת ($\kappa$) בנקודות התקפות, ביחידות $1/\text{pixel}$.
* `max_curvature`: עקמומיות מקסימלית לאורך החלקים התקפים של המסלול.
* `p95_curvature`: אחוזון 95 של העקמומיות (עמיד יותר לרעשי קצה מ-max).
* `curvature_std`: סטיית התקן של העקמומיות. משקפת האם העקמומיות אחידה (כמו בקשת מעגלית או בזייה חלקה) או משתנה (כמו בתנועה אנושית הכוללת תיקוני מסלול).
* `curvature_coverage_pct`: אחוז אורך המסלול שבו העקמומיות חושבה בצורה תקפה. כברירת מחדל, גם במסלול נקי הכיסוי המרבי הוא כ-**93.94%** בגלל השמטת שולי החלון בקצוות.
* `curvature_used_points` / `curvature_total_points`: מספר מרכזי החלונות שנכללו בחישוב ביחס לסך הנקודות הפנימיות.
* `curvature_status`:
  * `estimated`: כל החלונות הפנימיים נכללו בחישוב.
  * `partial_estimate`: חלונות מסוימים הוחרגו (עקב פניות חדות מדי או מרווחים גדולים), והמדדים מבוססים על החלקים הנותרים בלבד.
  * `unavailable`: פחות מ-7 נקודות תנועה או שאין רצף נקודות מחובר.

### 4. מדדי כיוון וזוויות
* `total_angle_change`: סכום שינויי הזווית המוחלטים המצטברים ברדיאנים: $\sum |\text{wrap}(\Delta \theta_i)|$. מדד זה רגיש לקוונטיזציית פיקסלים בדידים ($\mathbb{Z}^2$).

---

## 🧮 אלגוריתם חישוב העקמומיות (`local_mask_v1`)

1. **סינון נקודות סטטיות:** דגימות רצופות זהות מוסרות מהעותק הגאומטרי (הנתונים המקוריים עם זמני השהייה נשמרים לחישובי זמנים).
2. **דגימה מחדש לפי אורך קשת (Arc-Length Resampling):**
   * מחושב אורך הקשת המצטבר לאורך המסלול: $s_k = \sum_{j=1}^k \|\mathbf{p}_j - \mathbf{p}_{j-1}\|$.
   * מבוצעת אינטרפולציה לינארית ל-100 נקודות במרווח מרחבי שווה: $ds = \frac{s_n}{99}$.
3. **התאמת פולינום מקומי ממעלה 3 בחלון נע (Local Cubic Fit):**
   * עבור כל נקודת מרכז בחלון של 7 נקודות ($[-3, -2, -1, 0, 1, 2, 3]$), מותאם פולינום מעלה שלישית באמצעות ריבועים פחותים (`np.linalg.lstsq`):
     $$x(u) = a_0 + a_1 u + a_2 u^2 + a_3 u^3, \quad y(u) = b_0 + b_1 u + b_2 u^2 + b_3 u^3$$
   * הנגזרת הראשונה והשנייה במרכז החלון ($u=0$) מחושבות אנליטית:
     $$\mathbf{r}'(0) = \frac{1}{ds} (a_1, b_1), \quad \mathbf{r}''(0) = \frac{2}{ds^2} (a_2, b_2)$$
4. **חישוב עקמומיות פרמטרית:**
   $$\kappa = \frac{|x' y'' - y' x'|}{(x'^2 + y'^2)^{3/2}}$$
5. **מנגנון מסכות מקומיות (Exclusion Masks):**
   * **שולי קצוות (Endpoints):** מחצית מרוחב החלון מכל קצה אינה מחושבת.
   * **מרווחים גדולים (Sparse Gaps):** אם קיים מרווח גולמי הגדול מ-80 פיקסלים או $2.5 \times \text{span}$, כל חלון שנוגע במרווח מוחרג.
   * **היפוכי כיוון מדויקים (Reversals):** פניות חדות של $\approx 180^\circ$ מוחרגות יחד עם סביבתן כדי למנוע ערכים סינגולריים מלאכותיים.

---

## 🧪 הרצה ובדיקות

```bash
# הרצת הניתוח על תיקיית הנתונים:
python3 analyze_trajectories.py

# בדיקת תקינות על צורות גאומטריות מדויקות (קו ישר וקשת עגולה):
python3 analyze_trajectories.py --validate

# הרצת חבילת בדיקות היחידה המקיפה:
python3 -m unittest test_analyze_trajectories.py test_mouse_bots.py
```
