# ניתוח המסלולים

הפעילי את `analyze_trajectories.py` מתוך סביבת העבודה כמו את קובץ האיסוף. הוא קורא את תיקיית `data` שלצדו ואת `session_*` שבתוכה, ומפיק לצדו `geometry_summary.csv`. אין צורך להפעיל את מערכת האיסוף לצורך הניתוח. ההקלטות המקוריות לא משתנות. הסיכום מתעדכן בכל הרצה.

כל שורה בסיכום מייצגת קובץ מסלול. `session_id`, `participant_id` ו־`source_file` מאפשרים להבחין בין קבצים בעלי אותו שם בהפעלות שונות. המזהה בפרויקט הנוכחי מתקדם בכל מסלול מוצלח ואינו מזהה בהכרח אדם ייחודי.

עמודת `source_type` מבדילה בין אדם (`human`) ושלושת הבוטים (`bot_linear`, `bot_curved`, `bot_noisy`). הקלטות ישנות בפרויקט ללא העמודה מסומנות כאדם, בהתאם למקורן לפני הוספת הבוטים. יש להשתמש בסוג המקור כתווית הניסוי, לא כתכונה שעל פיה מסווגים תנועה. גם מזהים, שמות קבצים, seed ופרמטרי המחולל אינם תכונות תנועה למסווג.

## הפעלת הבוטים

שלושת הכפתורים נמצאים בתוך `mouse_tracker.py`, בחלון האיסוף הרגיל. אין צורך בקובץ בוטים נפרד. לחיצה על כפתור מתחילה ציור והקלטה בזמן אמת. לא ניתן להתחיל הקלטה שנייה עד לסיום או ביטול הראשונה. כפתור ביטול מפסיק את הניסיון ללא שמירה או קידום המזהה; תזוזת העכבר הפיזי מחוץ למשטח אינה מבטלת בוט, אך ממשיכה לבטל הקלטה אנושית.

הבוט הליניארי מתקדם בקו ישר במהירות קבועה ברמת המסלול הרציף. המעוקל משתמש ב־Bézier ריבועי עם נקודת בקרה בניצב לכיוון התנועה ובתוך המשטח. הרועש משתמש בסטיות צידיות רציפות ובחזרה הדרגתית לקו. ההתחלה נבחרת בתוך הירוק, והסיום הוא הדגימה הראשונה בתוך האדום, כמו כלל הסיום לאדם. מיקומי הבוט נרשמים בפיקסלים שלמים כמו מיקום העכבר; לכן גם בוט ליניארי עשוי להציג שינויי זווית קטנים בשל עיגול לפיקסלים.

כל ההקלטות משתמשות באותו שעון ובבקשת דגימה כל 5 מילישניות. הזמנים בפועל נשמרים; התזמון אינו מובטח וזהה בדיוק בין ריצות. ברירת המחדל של כל שלושת הבוטים היא שנייה אחת לתנועה עד מרכז המטרה, אבל ההקלטה מסתיימת מוקדם יותר כשהיא נדגמת בתוך העיגול. זו ברירת מחדל לבוטי בסיס, לא התאמה שכבר כוילה למשכי תנועה אנושיים. לכל ניסיון יש אותה בחירה אקראית של מיקומי מטרות, אך ריצות נפרדות אינן זוגות עם תנאים זהים. ניסוי השוואתי מבוקר עדיין דורש התאמת מרחקים, משכים ותנאי התחלה בין המקורות.

בנוסף לעמודות הקיימות נשמרים `source_type`, `bot_seed`, `planned_duration_s`, `control_x`, `control_y`, `noise_amplitude_px` ו־`generator_version`. השדות המיועדים לבוט נשארים ריקים אצל אדם. seed קובע את התוכנית הגאומטרית; לשחזור הדגימות עצמן נדרשים גם הזמנים בפועל ומיקומי העיגולים. קובצי המקור נשמרים באותו session ובאותה סדרת שמות P01, P02 וכן הלאה; סוג המקור נשמר בעמודה מפורשת.

## המדדים

- `path_length`: אורך המסלול הגולמי בפיקסלים, ללא שינוי בעקבות ההחלקה.
- `direct_distance`: המרחק בין נקודת ההקלטה הראשונה לאחרונה. `target_center_distance` הוא המרחק הנפרד בין מרכזי העיגולים.
- `path_ratio`: אורך המסלול חלקי המרחק הישיר. תא ריק כשנקודות ההתחלה והסיום זהות.
- `duration_s`, `mean_speed_px_s`: משך ההקלטה והמהירות הממוצעת, כולל העצירות, לפי הזמנים האמיתיים.
- `stationary_interval_s`: סכום מרווחי הזמן שבשני קצותיהם אותו מיקום. זו הערכה מתוך הדגימות, לא הוכחה שלא הייתה תנועה ביניהן.
- `median_sample_interval_ms`, `max_sample_interval_ms`: המרווח החציוני והמרבי בין דגימות, במילישניות.
- `total_angle_change`: סכום שינויי הכיוון המוחלטים במסלול הגולמי, ברדיאנים. רגיש לרעש פיקסלים ולצפיפות הדגימה.
- `mean_curvature`, `max_curvature`, `p95_curvature`: אומדני עקמומיות מוחלטת ביחידות 1/פיקסל, בנקודות שנותרו לאחר ההשמטה המקומית בלבד. הממוצע והאחוזון מחושבים על מרכזי חלונות במרווח מרחבי אחיד. אין לפרש אותם כמדדי המסלול כולו כשהכיסוי חלקי.
- `curvature_coverage_pct`: אחוז אורך המסלול שבין מרכזי חלונות סמוכים שנכללו שניהם בחישוב. לא מגשרים מעל נקודות שהושמטו, ונקודה בודדת ללא שכנה לא נכללת. גם ללא בעיות הכיסוי בברירות המחדל הוא כ־93.94%, בגלל הקצוות שאינם מחושבים.
- `curvature_used_points`, `curvature_total_points`: מספר מרכזי החלונות שנכללו ומספרם לפני ההשמטות המקומיות.
- `curvature_reversal_count`, `curvature_sparse_gap_count`: מספר היפוכי הכיוון המדויקים ומרווחי המקור הגדולים שזוהו, גם כששני הסוגים מופיעים באותו מסלול.
- `curvature_excluded_ranges`: רשימת JSON של טווחים שהובילו להשמטה וסיבתם. המיקום הוא מרחק מצטבר בפיקסלים לאורך המסלול, לא זמן. טווחים יכולים לחפוף; אין לסכום את אורכיהם לחישוב הכיסוי.
- `curvature_method`: מזהה שיטת החישוב; הגרסה הנוכחית היא `local_mask_v1`.

## איך מחושבת העקמומיות

מסירים נקודות רצופות זהות בעותק גאומטרי בלבד. נתוני הזמן המלאים נשארים לחישובי הזמן. מבצעים אינטרפולציה לינארית ל־100 נקודות במרחקים שווים לאורך המסלול, ומתאימים פולינום מקומי ממעלה שלישית בחלון של 7 נקודות. מחשבים נגזרות מהפולינום במרכז החלון ומוותרים על שלוש נקודות הקצה בכל צד. התוצאה היא אומדן של החלק הפנימי של המסלול לאחר החלקה, ולא עקמומיות מדויקת של כל המסלול המקורי.

`smoothing_span_px` שומר את גודל חלון ההחלקה בפיקסלים. החלון תלוי באורך המסלול ולכן יש להביא אותו בחשבון בהשוואות. שינוי `--points` או `--window` משנה את האומדן; 100 ו־7 הם ברירות מחדל לבדיקה, לא פרמטרים שכבר אומתו למחקר. אינטרפולציה והחלקה אינן משחזרות פרטים שלא נדגמו.

כאשר מרווח מקור גדול מחלון ההחלקה, מושמטים כל חלונות החישוב שנוגעים במרווח — גם אם מרכז החלון מחוץ לו. סביב היפוך כיוון מדויק מושמט חצי חלון לכל צד. השמטות של שני הסוגים מצטרפות, בלי למחוק או להזיז את דגימות המקור. נגזרות מקומיות לא מוגדרות אינן נכללות. בשאר החלקים משתמשים באותה שיטת התאמת פולינום ובאותם פרמטרים שהיו לפני השינוי.

זהו ניסיון להגביל את החישוב לאזורים שיש להם תמיכה לפי הכללים הממומשים, לא הוכחה לדיוקם. אין סף כיסוי מחקרי שנבחר עדיין: גם תוצאה עם כיסוי נמוך עשויה להופיע, אבל מסומנת כחלקית וחייבים לבדוק את הכיסוי לפני השוואה בין מסלולים. הסרת אזור הפנייה יכולה להפחית את המקסימום; אין להסיק מכך שכל המסלול נעשה ישר יותר.

השיטה משתמשת ב־[NumPy interp](https://numpy.org/doc/stable/reference/generated/numpy.interp.html) וב־[NumPy least squares](https://numpy.org/doc/stable/reference/generated/numpy.linalg.lstsq.html).

## סטטוסים

- `ok`: החישובים עברו את הבדיקות הממומשות; אינו אישור לאיכות מדעית של מסלול.
- `review`: יש הערה בעמודת `notes`; ייתכן שרק חלק מהמדדים דורשים בדיקה.
- `invalid`: מבנה או תוכן לא תקינים; אין להשתמש בשורה לניתוח בלי לתקן את המקור.

`curvature_status` מפרט בנפרד:

- `estimated`: כל מרכזי החלונות הפנימיים נכללו; קצוות המסלול עדיין אינם נכללים.
- `partial_estimate`: מדדים חושבו רק על החלקים שנותרו אחרי השמטת אזורים בעייתיים. הסטטוס הכללי נשאר `review`; הסיבות והכיסוי נשמרים בנפרד.
- `unavailable`: אין מספיק נקודות תנועה, או שלא נשארו מרכזי חלונות סמוכים מתאימים לחישוב. מדדי העקמומיות ריקים. נדרשות לפחות 7 נקודות תנועה לצורך השיטה; עמידה בסף אינה מבטיחה איכות.

`timing_status` מבדיל בין זמן תקין, זמן לא תקין וזמן שלא קיים. מדד שלא ניתן לחשב נשמר כתא ריק, לא כאפס.

## בדיקות והרצה

תלויות: Python, NumPy ו־pandas, שכבר זמינים בסביבת הפרויקט שנבדקה.

```sh
python3 analyze_trajectories.py
python3 analyze_trajectories.py --validate
python3 -m unittest test_analyze_trajectories.py
```

האימות כולל קו ישר, קשת, עצירות, זמנים לא אחידים או לא תקינים, חזרה לנקודת ההתחלה, היפוך כיוון, קבצים שגויים, שינוי קנה מידה וזיהוי sessions. נוספו בדיקות לשמירת חלקי מסלול משני צדי היפוך, להשמטת כל חלון שנוגע במרווח מקור ולחישוב הכיסוי ללא גישור מעל חורים. כל בדיקות הדוגמה משתמשות בקבצים זמניים.
