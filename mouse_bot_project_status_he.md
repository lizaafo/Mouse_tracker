# מצב פרויקט זיהוי בוטים לפי תנועת העכבר
### סיכום התקדמות עדכני, ממצאים ותוכנית עבודה | גאומטריה יישומית

הפרויקט השלים בהצלחה את **שלבים 1 עד 5 במלואם**:
1. מערכת איסוף גרפית מלאה מבוססת Tkinter.
2. מחולל של חמישה סוגי בוטים מתמטיים וביומכניים בזמן אמת.
3. מנוע חישובים גיאומטריים וקינמטיים עם אומדן עקמומיות מקומית בחלון נע ופרופילי מהירות.
4. מערך ויזואליזציה המפיק 4 גרפים השוואתיים באיכות פרסום.
5. מודל סיווג ולמידת מכונה (עצי החלטה בינאריים ומרובי מחלקות) המפריד בין מסלולים אנושיים לבוטים.

כעת הפרויקט עובר לשלב הסיכום, הרחבת הנתונים עבור הבוטים החכמים, וחיבור מעמיק לתיאוריה של **חוק שני השלישים** לקראת הגשת הדוח הסופי.

---

### מה כבר הושלם ועובד בפועל

#### 1. מערכת האיסוף והממשק (`mouse_tracker.py`)
* ✓ **סביבת איסוף חלקה:** ממשק גרפי (Tkinter) בגודל 900x600 פיקסלים עם מעגל התחלה ירוק ומעגל מטרה אדום.
* ✓ **דגימה היברידית ברזולוציה גבוהה:** שילוב של פולינג רציף כל 5ms ואירועי חומרה ישירים של העכבר (`<Motion>`) לקליטת תנועות מהירות ללא שיהוי.
* ✓ **ניהול הפעלות (Sessions):** חלוקה אוטומטית לתיקיות הפעלה נפרדות (`session_01`, `session_02` וכו') ללא דריסת נתונים קודמים.
* ✓ **גיוון מרחבי:** הגרלה אקראית של מיקומי ההתחלה והמטרה בכל ניסיון (מרחק מינימלי 250 פיקסלים ושוליים) לקבלת זוויות ומרחקים מגוונים.
* ✓ **זיהוי חציית מטרה מהירה:** בדיקת חיתוך גיאומטרי של קטע התנועה עם מעגל המטרה (`segment_intersects_circle`), המונעת פספוס סיום בתנועות מהירות במיוחד.
* ✓ **מטא-נתונים עשירים בכל דגימה:** שמירת מקור המסלול, זרע אקראי, מימדי משטח, זמנים ופרמטרי בקרה.

#### 2. מחולל הבוטים המובנה (5 סוגי בוטים ב-`mouse_tracker.py`)
* ✓ **בוט ליניארי (`bot_linear`):** תנועה בקו ישר במהירות אחידה.
* ✓ **בוט מעוקל (`bot_curved`):** תנועה חלקה לאורך עקומת בזייה ריבועית (Bézier) במהירות פרמטרית אחידה.
* ✓ **בוט רועש (`bot_noisy`):** תנועה עם הפרעות צידיות אקראיות רציפות (אינטרפולציית Smoothstep) ותיקון הדרגתי ליעד.
* ✓ **בוט Min-Jerk (`bot_smart_jerk`):** תנועה המבוססת על מודל Minimum Jerk (Flash & Hogan, 1985) עם פולינום מעלה 5 המדמה האצה ובלימה אנושית חלקה לאורך קשת בזייה.
* ✓ **בוט ביומכני (`bot_smart_full`):** מודל מתקדם המשלב פרופיל מהירות א-סימטרי ($\tau^{0.8}$ – שיא מהירות מוקדם), בלימה מוארכת ורעידות נוירו-מוטוריות פיזיולוגיות (Tremor בתדרי 10Hz ו-18Hz).

#### 3. מנוע החישובים הגיאומטריים והקינמטיים (`analyze_trajectories.py`)
* ✓ **דגימה מרחבית שווה:** אינטרפולציה ל-100 נקודות במרווחי אורך קשת אחידים לאורך המסלול.
* ✓ **אומדן עקמומיות אנליטי:** התאמת פולינום מעלה 3 בחלון נע של 7 נקודות באמצעות ריבועים פחותים (`np.linalg.lstsq`), המונעת רעשי גזירה נומרית כפולה.
* ✓ **מסכות הגנה וכיסוי:** זיהוי והחרגה של היפוכי כיוון חדים (Reversals), מרווחי דגימה חסרים (Gaps), ושולי קצוות.
* ✓ **מדדי ישרות מתקדמים:** חישוב אורך דרך, מרחק ישיר, יחס ישרות (`path_ratio`), וסטייה מקסימלית מהמיתר (`max_chord_dev_px`).
* ✓ **מדדים קינמטיים:** חישוב זמני שהייה ועצירה (`stationary_interval_s`), מהירות ממוצעת, ויחס שיא המהירות למהירות הממוצעת (`peak_to_mean_speed`).
* ✓ **טבלת סיכום מרכזת:** הפקה אוטומטית של `geometry_summary.csv` עבור כלל המסלולים בכל ההפעלות.

#### 4. מודל הסיווג ולמידת המכונה (`classify_trajectories.py`)
* ✓ **סיווג בינארי (Human vs. Bot):** עץ החלטה (Decision Tree) המפריד בדיוק גבוה בין תנועות אנושיות לבוטים על בסיס פרופיל המהירות וישרות המסלול.
* ✓ **סיווג מרובה מחלקות (Multiclass):** עץ החלטה המזהה את סוג התנועה הספציפי (`human`, `linear`, `curved`, `noisy`).
* ✓ **הערכת ביצועים מלאה:** חישוב Train/Test Split, Accuracy, מטריצת בלבול (Confusion Matrix), ודוח סיווג מפורט.

#### 5. מערך הוויזואליזציה (`visualize_results.py`)
* ✓ הפקת 4 גרפים ברזולוציה גבוהה השמורים בתיקיית `plots/`:
  1. `trajectories_comparison.png`: השוואת 4 מסלולים מייצגים במישור מול המיתר הישיר.
  2. `features_scatter.png`: תרשים פיזור המציג את ההפרדה בין המקורות לפי מאפייני ישרות ופרופיל מהירות.
  3. `velocity_profiles.png`: פרופילי מהירות לאורך זמן המדגימים את צורת הפעמון האנושית מול מהירות הבוטים.
  4. `decision_tree_diagram.png`: תרשים גרפי מלא של עץ ההחלטה שנלמד.

---

### מצאי הנתונים שנאספו בפועל בפרויקט

נכון לעכשיו, נאספו ועובדו **52 מסלולים** בחלוקה הבאה:

| סוג מקור התנועה | מזהה מערכת | כמות מסלולים | מאפיין עיקרי בנתונים |
| :--- | :--- | :---: | :--- |
| **אדם** | `human` | **20** | פרופיל מהירות פעמוני מובהק, תיקוני כיוון עדינים, זמני שהייה קצרים. |
| **בוט רועש** | `bot_noisy` | **12** | סטיות ניצבות של עד 12 פיקסלים, שינויי זווית גבוהים, מהירות אחידה. |
| **בוט מעוקל** | `bot_curved` | **9** | עקמומיות חלקה, סטיית מיתר גדולה (מעל 30 פיקסלים), מהירות אחידה. |
| **בוט ליניארי** | `bot_linear` | **6** | קו ישר כמעט מושלם (סטיית מיתר < 1 פיקסל), יחס דרך $\approx 1.0$. |
| **בוט Min-Jerk** | `bot_smart_jerk` | **3** | פרופיל מהירות חלק עם שיא במרכז ($peak/mean \approx 1.88$), קשת חלקה. |
| **בוט ביומכני** | `bot_smart_full` | **2** | פרופיל א-סימטרי עם שיא מהירות מוקדם, בלימה מוארכת ורעידות 10Hz. |
| **סה"כ מסלולים** | | **52** | **בסיס נתונים רחב המכסה את כל 5 המודלים** |

---

### ממצאים מרכזיים והשוואה גיאומטרית-קינמטית

1. **כושר ההפרדה של פרופיל המהירות (`peak_to_mean_speed`):**
   * בבוטים הנאיביים (`linear`, `curved`, `noisy`), המהירות קבועה לחלוטין ולכן היחס קרוב ל-**1.0**.
   * בתנועה אנושית, קיים פרופיל האצה ובלימה מובהק (התואם את מודל ה-Minimum Jerk) שבו היחס נע בין **2.0 ל-2.6**.
   * בוט ה-Min-Jerk והבוט הביומכני מייצרים יחס של כ-**1.88**, מה שמקשה על ההפרדה הפשוטה ומחייב שימוש במדדי שונות עקמומיות ורעידות.

2. **כושר ההפרדה של מדד הסטייה מהמיתר (`max_chord_dev_px`):**
   * מבדיל בצורה חדה וברורה בין בוט ליניארי (פחות מ-1 פיקסל) לבין בוט רועש (5–20 פיקסלים) ובוט מעוקל (מעל 25 פיקסלים).

3. **שונות העקמומיות (`curvature_std`):**
   * בוט מעוקל מייצר קשת בזייה חלקה בעלת שונות עקמומיות נמוכה ומסודרת, בעוד שאצל אדם קיימים מיקרו-תיקונים המגדילים את שונות העקמומיות לאורך הדרך.

---

### תוכנית עבודה לסבב הסופי לקראת הגשה

1. **הרחבת דגימות לבוטים הביומכניים:**
   * איסוף של 15–20 דגימות מכל אחד משני הבוטים המתקדמים (`bot_smart_jerk` ו-`bot_smart_full`) כדי להעמיד את מודל ה-ML במבחן מול בוטים המחקים תנועה אנושית.
2. **יישום חוק שני השלישים (Two-Thirds Power Law):**
   * הוספת מדד ב-`analyze_trajectories.py` המודד את הקורלציה בין מהירות רגעית לרדיוס עקמומיות ($v \propto R^{1/3}$). זהו הקשר הגאומטרי-קינמטי המרכזי ביותר להוכחת אנושיות התנועה.
3. **דינמיזציה של הוויזואליזציה:**
   * עדכון סקריפט הגרפים שיקרא את ספי ההחלטה ישירות מעץ ההחלטה המאומן במקום קווים מקודדים קשיח.
4. **כתיבת הדוח המסכם ומצגת:**
   * שילוב 4 הגרפים שנוצרו ב-`plots/` בתוך הדוח הסופי.
   * קישור הממצאים לתיאוריית הקורס (עקומות, עקמומיות, מסלולי Dubins ומינימום ג'רק).
