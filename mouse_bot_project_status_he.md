# מצב פרויקט זיהוי בוטים לפי תנועת העכבר
### סיכום התקדמות עדכני ותוכנית עבודה לסבב הבא | גיאומטריה יישומית

הפרויקט השלים קפיצת מדרגה משמעותית: שלבים 1, 2 ו-3 מתוך תוכנית העבודה המקורית הושלמו במלואם. מערכת איסוף הנתונים שודרגה עם ניהול הפעלות עצמאיות, הגרלת מיקומים, מנגנון למניעת פספוס מעבר מהיר ושמירת מטא-נתונים עשירה. בנוסף, מומשו בממשק שלושה סוגי בוטים מתמטיים הפועלים בזמן אמת, והוקם מנוע חישובים גיאומטריים שלם שמחלץ שורה ארוכה של מדדי דרך, זוויות ועקמומיות מקומית. כעת הפרויקט ערוך לכניסה לשלבים 4 ו-5: איסוף מורחב, השוואת מדדים ופיתוח מנגנון הסיווג והזיהוי בין אדם לבוט.

---

### מה כבר הושלם ועובד בפועל

#### 1. מערכת האיסוף והממשק (`mouse_tracker.py`)
* ✓ **סביבת איסוף:** ממשק גרפי מבוסס Tkinter בגודל 900x600 פיקסלים עם עיגול התחלה ירוק ועיגול מטרה אדום.
* ✓ **דגימה ותצוגה:** דגימת מיקום העכבר כל 5 מילישניות וציור חי של המסלול בזמן אמת.
* ✓ **ניהול הפעלות (Sessions):** חלוקה אוטומטית לתיקיות הפעלה נפרדות (`session_01` עד `session_06`) ללא דריסת נתונים קודמים.
* ✓ **גיוון מרחבי:** הגרלה אקראית של מיקומי ההתחלה והמטרה בכל ניסיון (מרחק מינימלי 250 פיקסלים ושוליים) לקבלת זוויות ומרחקים מגוונים.
* ✓ **חציית מטרה מהירה:** זיהוי חיתוך גיאומטרי של קו התנועה עם מעגל המטרה, המונע פספוס סיום בתנועות מהירות במיוחד.
* ✓ **פסילת ניסיונות:** ביטול אוטומטי של ניסיון אנושי במקרה של יציאת הסמן מגבולות הקנבס, כדי לשמור על איכות הנתונים.
* ✓ **מטא-נתונים עשירים:** שמירת מטא-נתונים מלאים בכל קובץ CSV: סוג מקור, זרע אקראי, מימדי משטח, זמנים ופרמטרי בקרה.

#### 2. מחולל הבוטים המובנה (`mouse_tracker.py`)
* ✓ **בוט ליניארי (`bot_linear`):** מתקדם בקו ישר במהירות קבועה עד לחציית מעגל המטרה.
* ✓ **בוט מעוקל (`bot_curved`):** מתקדם לאורך עקומת Bézier ריבועית חלקה עם נקודת בקרה ניצבת לכיוון התנועה.
* ✓ **בוט רועש (`bot_noisy`):** מתקדם תוך הוספת סטיות צידיות אקראיות רציפות ותיקון הדרגתי לעבר היעד.
* ✓ **שילוב בממשק:** שלושת הבוטים מופעלים ישירות מכפתורים בממשק ונשמרים באותו מבנה נתונים וקצב דגימה של משתמש אנושי.

#### 3. מנוע החישובים הגיאומטריים (`analyze_trajectories.py`)
* ✓ **עיבוד מקדים:** סינון נקודות רצופות זהות ואינטרפולציה ל-100 נקודות במרווח מרחבי אחיד.
* ✓ **מדדי מרחק:** חישוב אורך מסלול גולמי (`path_length`), מרחק ישיר (`direct_distance`) ויחס דרך (`path_ratio`).
* ✓ **שינויי כיוון:** חישוב סך שינויי כיוון מצטברים (`total_angle_change`) ברדיאנים לאורך המסלול הגולמי.
* ✓ **מדדי זמן ומהירות:** חישוב משך התנועה (`duration_s`), מהירות ממוצעת (`mean_speed_px_s`) וזמני עצירה (`stationary_interval_s`).
* ✓ **חישוב עקמומיות:** אומדן עקמומיות מקומית (`mean`, `max`, `p95`) באמצעות התאמת פולינום מעלה 3 בחלון נע של 7 נקודות.
* ✓ **אמינות וכיסוי:** זיהוי והחרגה של היפוכי כיוון (Reversals) ומרווחי דגימה חסרים (Sparse Gaps), עם חישוב אחוז כיסוי עקמומיות (`curvature_coverage_pct`).
* ✓ **קובץ סיכום מרוכז:** הפקה אוטומטית של טבלה מסכמת `geometry_summary.csv` עבור כלל המסלולים בכל ההפעלות.
* ✓ **אימות ובדיקות:** קובצי בדיקות יחידה מקיפים `test_analyze_trajectories.py` ו-`test_mouse_bots.py`.

---

### מבנה הנתונים ומצאי המסלולים הקיים

להלן מבנה השדות שנשמרים בכל קובץ CSV של מסלול בודד:

| trajectory_id | participant_id | sample_index | x | y | time | source_type | bot_seed |
| :---: | :---: | :---: | :---: | :---: | :---: | :---: | :---: |
| מזהה ייחודי | P01... | מספר רץ | פיקסלים | פיקסלים | שניות | human / bot_* | ערך מספרי (לבוט) |

<br>

בקובץ הסיכום `geometry_summary.csv` מחושבים המדדים הגיאומטריים המרכזיים:

| קבוצת מדדים | שדות מרכזיים | תיאור ומשמעות לזיהוי |
| :--- | :--- | :--- |
| **מדדי ישרות ודרך** | `path_length`, `direct_distance`, `path_ratio` | יחס הדרך קרוב ל-1 בבוט ליניארי, וגבוה יותר באדם ובבוט רועש. |
| **מדדי כיוון וזווית** | `total_angle_change` | רגיש לרעש ולשינויי כיוון: נמוך במיוחד בבוט ליניארי, גבוה בבוט רועש. |
| **מדדי עקמומיות** | `mean_curvature`, `max_curvature`, `p95_curvature` | עקמומיות חלקה בבוט מעוקל לעומת עקמומיות משתנה ולא אחידה בתנועת יד אנושית. |
| **מדדי זמן ומהירות** | `duration_s`, `mean_speed_px_s`, `stationary_interval_s` | בוטים נעים במהירות אחידה וקבועה; בני אדם מציגים האצות, בלימות ועצירות קצרות. |
| **איכות וכיסוי** | `curvature_coverage_pct`, `status` | אימות תקינות החישוב ואחוז המסלול שבו העקמומיות הוגדרה באופן תקף. |

<br>

מצאי הנתונים שנאספו בפועל בפרויקט עד כה:

| תיקיית הפעלה (Session) | מספר מסלולים | מקורות המסלול שנכללו | סטטוס עיבוד בסיכום |
| :--- | :---: | :--- | :--- |
| **`session_01` עד `session_05`** | 24 מסלולים | אדם (`human`) | מעובד ומסוכם ב-`geometry_summary.csv` |
| **`session_06` (חדש)** | 7 מסלולים | אדם (1), בוט ליניארי (2), בוט מעוקל (2), בוט רועש (2) | נאסף בהצלחה; ממתין לעדכון סקריפט הניתוח |
| **סה"כ נתונים** | **31 מסלולים** | **25 מסלולי אדם + 6 מסלולי בוטים** | **בסיס התחלתי מוצק להרחבת הניסוי** |

---

### תוכנית עבודה לסבב העבודה הבא

בסבב הבא הפרויקט עובר משלב התשתית והחישובים (שלבים 1–3) לשלב הניסוי, ההשוואה והסיווג (שלבים 4–5):

#### שלב 4: הרחבת איסוף הנתונים וניתוח השוואתי
* • **הגדלת מאגר המסלולים:** איסוף 15–20 מסלולים מכל מקור (אדם, בוט ליניארי, בוט מעוקל, בוט רועש) במספר הפעלות שונות.
* • **עדכון קובץ הסיכום:** הרצת `analyze_trajectories.py` על כלל הסשנים (כולל `session_06` והבאים) כך שקובץ הסיכום יכלול את כל הבוטים.
* • **ניתוח סטטיסטי השוואתי:** השוואת התפלגויות המדדים (עקמומיות, יחס דרך, שינויי זווית, מהירות) בין בני אדם לבוטים.
* • **ויזואליזציה של ההפרדה:** הפקת היסטוגרמות וגרפי Boxplot להמחשת כושר ההפרדה של כל תכונה גיאומטרית.

#### שלב 5: מנגנון הזיהוי והסיווג (היעד המרכזי הבא)
* • **כלל סיווג גיאומטרי (Heuristic):** הגדרת ספים ברורים על המדדים הגיאומטריים (למשל: סף `total_angle_change` לזיהוי בוט ישר, סף שונות עקמומיות לזיהוי בוט מעוקל).
* • **הפרדת נתוני בדיקה:** חלוקת המאגר לקבוצת אימון/כיול וקבוצת בדיקה (Train/Test Split) לבדיקת עמידות המודל.
* • **הערכת ביצועי הסיווג:** מדידת Accuracy, Precision, Recall, ודגש מיוחד על צמצום False Positives (סיווג שגוי של אדם כבוט).
* • **מודל למידת מכונה פשוט:** אימון מודל פשוט (עץ החלטה – Decision Tree, או רגרסיה לוגיסטית) והשוואת ביצועיו לכלל הגיאומטרי.

#### שלב 6: ממשק הדגמה, דוח סופי ומצגת
* • **חיווי חי בממשק:** הצגת קביעת הסיווג ('אדם' / 'בוט') וערכי המדדים ישירות בממשק מיד בסיום התנועה.
* • **כתיבת הדוח המתמטי:** חיבור הממצאים הניסיוניים לתיאוריה הגיאומטרית של הקורס (עקומות, עקמומיות ומסלולי Dubins מתוך סיכומי המאמרים).
* • **מצגת והדגמה חיה:** הכנת מצגת לקורס והדגמה חיה המציגה משתמש אנושי מול שלושת סוגי הבוטים בזמן אמת.

---

### משימות מומלצות בעדיפות מיידית לסבב הבא

| עדיפות | משימה | פעולה נדרשת | תוצאה מצופה |
| :---: | :--- | :--- | :--- |
| **1** | **עדכון קובץ הסיכום** | הרצת `analyze_trajectories.py` על `session_06` והסשנים החדשים | `geometry_summary.csv` מעודכן הכולל גם בוטים |
| **2** | **איסוף דאטה מורחב** | ביצוע ניסויי איסוף נוספים (אדם + 3 בוטים במרחקים שונים) | מאגר מאוזן של 15–20 דגימות מכל מקור |
| **3** | **השוואה סטטיסטית וגרפים** | כתיבת סקריפט גרפים (Boxplots והיסטוגרמות) למדדי הפיצ'רים | זיהוי ויזואלי של המדדים המבדילים ביותר |
| **4** | **בניית כלל סיווג ראשוני** | ניסוח כלל החלטה מבוסס ספים על `total_angle_change` ועקמומיות | סיווג ראשוני עובד ומדדי דיוק (Accuracy/Recall) |

---

### חלוקת אחריות מוצעת בצוות
* • **חבר צוות 1 – נתונים וממשק:** ביצוע הקלטות נוספות של משתמשים אנושיים, ניהול הסשנים ושילוב חיווי הסיווג בממשק Tkinter.
* • **חבר צוות 2 – ניתוח גיאומטרי וויזואליזציה:** עיבוד הנתונים המורחבים, הפקת טבלאות השוואה, גרפי מסלולים מייצגים והתפלגויות עקמומיות.
* • **חבר צוות 3 – מודל סיווג וכתיבת הדוח:** פיתוח כלל הסיווג ומודל ה-ML, מדידת דיוק, וכתיבת החלק המתמטי בדוח על בסיס סיכומי המאמרים.
